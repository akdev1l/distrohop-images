#### distrop-images: repository of container images made for cleanly testing other distros

1. Go into a directory for your distro of choice
2. Execute this from a tty:
```
$ sudo ./build.sh && sudo ./test.sh
```
